# 2026年1月19日 学习日志

### 1. 算法实战：二分查找 (LeetCode 704)
今天跟着代码随想录做了人生中的第一道算法题：二分查找！一开始自己做的时候还是有许多错误的

**第一轮代码**
```python
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        left = 0
        right = len(nums) # right边界没理清楚
        
        while left <= right # while没写冒号
            middle = left + (right - left) // 2
            if nums[middle] < target # if没写冒号 
                left = middle + 1
            else if nums[middle] > target # else if是c++里面的，python中是elif，并且没写冒号
                right = middle - 1
            else return middle # else也要写冒号
        return -1
```
**最终代码**
```python
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        left = 0
        right = len(nums) - 1
        while left <= right:
            middle = left + (right - left) // 2
            if nums[middle] < target:
                left = middle + 1
            elif nums[middle] > target:
                right = middle - 1
            else: 
                return middle
        return -1
```
### 2. 知识复盘：
* **防溢出**：要算 `(left + right) // 2` ， `left` 和 `right` 可能都很大，`left + right`就会溢出，则需要改写成 `left + (right - left) // 2` 来防止溢出。
* **时间空间复杂度**：简单了解了一下时间空间复杂度，比如一次 `for` 循环遍历时间复杂度是$O(n)$，两层嵌套 `for` 循环则是$O(n^2)$ ，二分查找，一次排除掉一半的则是$O(\log n)$。空间复杂度，输入不算，只用了几个变量（如 `left`，`right`， `mid`）：$O(1)$; 新建了一个和输入一样长的数组：$O(n)$。
* **python特性**: `//` 是整数除法， `while` 、`if`、 `else` 、 `elif` 要带`：`，`len()` 是python内置函数，求数组长度。