# 2026年1月19日 学习日志

### 1. 算法实战：二分查找 (LeetCode 704)
今天跟着代码随想录做了人生中的第一道算法题：二分查找！一开始自己做的时候还是有许多错误的

**第一轮代码**
```python
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        left = 0
        right = len(nums) # right边界没理清楚
        
        while left <= right # while没写冒号
            middle = left + (right - left) // 2
            if nums[middle] < target # if没写冒号 
                left = middle + 1
            else if nums[middle] > target # else if是c++里面的，python中是elif，并且没写冒号
                right = middle - 1
            else return middle # else也要写冒号
        return -1
```
**最终代码**
```python
class Solution:
    def search(self, nums: List[int], target: int) -> int:
        left = 0
        right = len(nums) - 1
        while left <= right:
            middle = left + (right - left) // 2
            if nums[middle] < target:
                left = middle + 1
            elif nums[middle] > target:
                right = middle - 1
            else: 
                return middle
        return -1
```
### 2. 知识复盘

* **防溢出**：计算 `(left + right) // 2` 时，如果 `left` 和 `right` 都很大，相加可能会溢出。应改写为 `left + (right - left) // 2` 来防止溢出。
* **复杂度分析**：
    * **时间复杂度**：单次 `for` 循环遍历是 $O(n)$，两层嵌套是 $O(n^2)$。二分查找由于每次排除一半，复杂度为 $O(\log n)$。
    * **空间复杂度**：输入数据不计算在内。只用了几个变量（如 `left`, `right`, `mid`）时为 $O(1)$；如果新建了一个和输入等长的数组，则为 $O(n)$。
* **Python 特性**：
    * `//` 是整数除法。
    * `while`, `if`, `else`, `elif` 后必须带冒号 `:`。
    * `len()` 是 Python 内置函数，用于求容器长度。